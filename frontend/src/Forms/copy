/* Removed max-tp:hidden for testing visibility */
        <form onSubmit={handleSubmit((data) => console.log(data))} className="w-175 flex flex-col gap-4 py-2 mt-2 ml-10">

            <div className="flex flex-col gap-3 ">

                {/* Title Section */}
                <div className="flex flex-col justify-center">
                    <div className="flex justify-between items-center">
                        <label className="label-editorial">Title</label>

                        {/* The Trigger: Just a label for the eyes, no click action */}
                        <div className="group relative flex-none w-auto">
                            <span className="label-editorial-hover cursor-pointer"> Prev. Title </span>
                            {/* THE REFERENCE TOOLTIP (Shows on Hover) */}
                            <div className="absolute right-0 w-75 mb-3 p-2 bg-gray-700 text-white text-[14px] rounded-md shadow-2xl opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none z-50 border border-white/10">
                                <p className="text-secondary font-medium uppercase tracking-normal mb-1 border-b-2 border-white/20 pb-1">
                                    Previous Title:
                                </p>
                                {/* This displays the data sitting in your 'selectedBook' state */}
                                <p className="leading-tight text-gray-300 italic">
                                    "{selectedBook?.title || "No previous title Found"}"
                                </p>


                            </div>
                        </div>
                    </div>

                    <input
                        {...register('title', {
                            required: "Required",
                            minLength: { value: 10, message: "Title has to be minimum 10 Characters Long" }
                        })}
                        className="block flex-1 border body-reading pl-2 py-1.5 rounded-md focus:ring-1 focus:ring-[#0a2463]"
                        placeholder="Edit Title"
                    />

                    {errors.title && <span className="text-red-500 font-md text-sm">{errors.title.message}</span>}
                </div>

                {/* description section */}
                <div className="flex flex-col justify-center">
                    <div className="flex justify-between items-center">
                        <label className="label-editorial">Description</label>

                        {/* The Trigger: Just a label for the eyes, no click action */}
                        <div className="group relative flex-none w-auto">
                            <span className="label-editorial-hover cursor-pointer"> Prev. Description </span>
                            {/* THE REFERENCE TOOLTIP (Shows on Hover) */}
                            <div className="absolute right-0 w-75 mb-3 p-2 bg-gray-700 text-white text-[14px] rounded-md shadow-2xl opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none z-50 border border-white/10">
                                <p className="text-secondary font-semibold uppercase tracking-normal mb-1 border-b-2 border-white/20 pb-1">
                                    Previous Description :
                                </p>
                                {/* This displays the data sitting in your 'selectedBook' state */}
                                <p className="leading-tight text-gray-300 italic">
                                    "{selectedBook?.description || "No previous description Found"}"
                                </p>


                            </div>
                        </div>
                    </div>

                    <input
                        {...register('description', {
                            required: "Required",
                            minLength: { value: 10, message: "Description has to be minimum 10 Characters Long" }
                        })}
                        className="block flex-1 border body-reading pl-2 py-1.5 rounded-md focus:ring-1 focus:ring-[#0a2463]"
                        placeholder="edit description"
                    />

                    {errors.description && <span className="text-red-500 font-md text-sm">{errors.description.message}</span>}
                </div>

                {/* Select Category */}
                <CategorySelector
                    register={register}
                    setValue={setValue}
                    control={control} // Get this from useForm()
                />

                {/* Price section */}
                <div className="flex flex-col justify-center">
                    <div className="flex justify-between items-center">
                        <label className="label-editorial">Price</label>

                        {/* The Trigger: Just a label for the eyes, no click action */}
                        <div className="group relative flex-none w-auto">
                            <span className="label-editorial-hover cursor-pointer"> Prev. Price </span>
                            {/* THE REFERENCE TOOLTIP (Shows on Hover) */}
                            <div className="absolute right-0 w-75 mb-3 p-2 bg-gray-700 text-white text-[14px] rounded-md shadow-2xl opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none z-50 border border-white/10">
                                <p className="text-secondary font-semibold uppercase tracking-normal mb-1 border-b-2 border-white/20 pb-1">
                                    Previous Price :
                                </p>
                                {/* This displays the data sitting in your 'selectedBook' state */}
                                <p className="leading-tight text-gray-300 italic">
                                    "{selectedBook?.price || "No previous price Found"}"
                                </p>


                            </div>
                        </div>
                    </div>

                    <input
                        {...register('price', {
                            required: "Required",
                            min: { value: 10, message: "Description has to be minimum 10 Characters Long" }
                        })}
                        className="block flex-1 border body-reading pl-2 py-1.5 rounded-md focus:ring-1 focus:ring-[#0a2463]"
                        placeholder="Edit Price"
                    />

                    {errors.price && <span className="text-red-500 font-md text-sm">{errors.price.message}</span>}
                </div>

                {/* Price section */}
                <div className="flex flex-col justify-center">
                    <div className="flex justify-between items-center">
                        <label className="label-editorial">Price</label>

                        {/* The Trigger: Just a label for the eyes, no click action */}
                        <div className="group relative flex-none w-auto">
                            <span className="label-editorial-hover cursor-pointer"> Prev. Price </span>
                            {/* THE REFERENCE TOOLTIP (Shows on Hover) */}
                            <div className="absolute right-0 w-75 mb-3 p-2 bg-gray-700 text-white text-[14px] rounded-md shadow-2xl opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none z-50 border border-white/10">
                                <p className="text-secondary font-semibold uppercase tracking-normal mb-1 border-b-2 border-white/20 pb-1">
                                    Previous Price :
                                </p>
                                {/* This displays the data sitting in your 'selectedBook' state */}
                                <p className="leading-tight text-gray-300 italic">
                                    "{selectedBook?.price || "No previous price Found"}"
                                </p>


                            </div>
                        </div>
                    </div>

                    <input
                        {...register('price', {
                            required: "Required",
                            min: { value: 10, message: "Description has to be minimum 10 Characters Long" }
                        })}
                        className="block flex-1 border body-reading pl-2 py-1.5 rounded-md focus:ring-1 focus:ring-[#0a2463]"
                        placeholder="Edit Price"
                    />

                    {errors.price && <span className="text-red-500 font-md text-sm">{errors.price.message}</span>}
                </div>
            </div>

            {/* Rest of your form components... */}
        </form>
    );
    }


      useEffect(() => {
    if (firstUnitCost) {
      setValue("costPrice", Number(firstUnitCost))
    }
  }, [firstUnitCost, setValue])

  useEffect(() => {
    if (isForRent) {
      setValue("rentalStatus", Rental_Tags.Status[0]);
    } else {
      setValue("rentalStatus", Rental_Tags.Status[3]);
      setValue("rentalPrice", 0);
    }
  }, [isForRent, setValue]);